function scoreTerritory(t){const count=activeSprays(t).length; const priority=t.priority||'ignorar'; if(priority==='ignorar')return -999; let score=0; if(priority==='mantener')score+=100; if(priority==='conquistar')score+=50; score+=(3-count)*25; if(Number(t.rep)===101)score+=10; if(count<3)score+=30; const expiries=activeSprays(t).map(s=>sprayExpires(s)-now()).sort((a,b)=>a-b); if(expiries[0]!==undefined && expiries[0]<CONFIG.criticalMs)score+=35; return score;}
function recommendations(){cleanExpired(); const arr=state.territories.filter(t=>t.priority!=='ignorar').map(t=>({...t,_score:scoreTerritory(t),_count:activeSprays(t).length})).sort((a,b)=>b._score-a._score); let slots=4, rec=[]; for(const t of arr){if(slots<=0)break; const need=Math.max(0,3-t._count); if(need===0)continue; const qty=Math.min(slots,need); rec.push({territory:t,qty,reason:t.priority==='mantener'?'Mantener territorio clave':'Subir influencia para conquista'}); slots-=qty;} return rec;}
